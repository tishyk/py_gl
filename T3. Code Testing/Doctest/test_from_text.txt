The ''example'' module
======================

Using ''factorial''
-------------------

This is an example text file in reStructuredText format.  First import
``factorial`` from the ``example`` module:

    >>> from doc_base_example import factorial

Now use it:
    >>> factorial(3)
    6
    >>> factorial(6)

    120
    >>> factorial(5)
    25